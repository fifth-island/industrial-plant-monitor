# API base URL — in dev the Vite proxy forwards /api to localhost:8000
# For production, set to the deployed backend URL
VITE_API_BASE_URL=/api/v1

# Production example (Render):
# VITE_API_BASE_URL=https://industrial-plant-monitor.onrender.com/api/v1
